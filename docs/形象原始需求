接下来将HomeScene(登录后的第一个页面)改成角色创建/选择界面。客户端表现如下：
1.设置昵称（即角色姓名，不同于登录用户名），可以随机，随机名搞一套常见的英文姓表+名表，随机组合+#四位数字
2.设置性别（男、女、无性）
3.形象创建，对于JavaME平台上，我的构思是这样的：
-创建一个绘图板比如32*64
-第一步  预设一套人物基本体态（动作+服饰），且将属于皮肤的色块坐标存为元数据。并且不同的体态但面部所在的位置都在同一套坐标下
-第二步  预设一套常见肤色色表（用户也可以调节三原色实现自定义肤色），根据第一步提到的肤色色块元数据，将肤色替换进去
-第三步  预设一套面板五官预设，因为第一步中不同基本形象的面部坐标位置一致，可以任意组合替换
-第四步  截取面部坐标，用户允许自己调色，对面部进行任意修改，甚至覆盖整个面部自行绘画五官
-第五步  装饰品，提供一套预设面部装备（如面具、创可贴等），可以装饰。也支持用户调色表，对全身形象进行绘制修改（第五步的操作因为是装饰品，所以在最后执行，因此图层也最高）


形象所需的美术资源我会想办法提供，请你总结后告诉我所需的图片规格、以及要设置哪些元数据，以实现上述机制功能
————————
客户端完成之后再设计服务端数据表和协议，可以放到下一截断完成